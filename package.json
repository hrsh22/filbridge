{
  "name": "autofi-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "sdk",
    "frontend",
    "backend",
    "test"
  ],
  "scripts": {
    "build:sdk": "cd sdk && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:sdk && npm run build:backend && npm run build:frontend"
  }
}

